string Email = email.Text;
            string Password = password.Text;
            string dbConnect = ConfigurationManager.ConnectionStrings["dbconnect"].ConnectionString;
            SqlConnection connect = new SqlConnection(dbConnect);

            connect.Open();
            string query = "select * from users where emailid='" + Email + "'and password='"+Password+"'";

            try
            {
                SqlCommand command = new SqlCommand(query, connect);
                command.Connection = connect;
                SqlDataAdapter data = new SqlDataAdapter(command);
                DataTable table = new DataTable();
                data.Fill(table);
                if (table.Rows.Count > 0)
                {
                    message1.Text = "Login Successfull";
                }
                else
                {
                    message1.Text = "Invalid Emailid or Password";
                }
            }
            catch (Exception)
            {
                message1.Text = "Exception";
            }
        }
        